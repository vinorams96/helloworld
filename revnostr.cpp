#include<iostream.h>

int main()
{
char str[20];
int i,len=0;

cout<<"\nENTER YOUR STRING:";
cin>>str;
if((str[0]>'A' && str[0]<'Z') && (str[0]>'a' && str[0]<'z')){
for(i=0;str[i]!='\0';i++)
{
len++;
}
for(;len>=z0;len--)
{
cout<<str[len];
}}
else
cout<<"Error";

return 0;
}

    Contact GitHub API Training Shop Blog About 

    © 2016 GitHub, I
